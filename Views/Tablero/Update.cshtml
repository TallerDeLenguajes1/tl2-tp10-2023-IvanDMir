@{ 
    ViewData["Title"] = "Actulizar Tablero";
}

@using tl2_tp10_2023_IvanDMir.ViewModels;
@model UBViewModel

<h1>@ViewData["Title"]</h1>
<form asp-controller="Tablero" asp-action="Update">
        <input hidden asp-for="@Model.Id">
    <div class="form-group">
        <label asp-for="@Model.Nombre">Nuevo nombre del Tablero</label>
        <input asp-for="@Model.Nombre" class="form-control" id="nuevoNombre">  
    </div>
    <div class="form-group">
        <label asp-for="@Model.Descripcion">Nueva Descripcion</label>
        <input asp-for = "@Model.Descripcion" class="form-control" id="Desc">  
    </div>
     <div class="form-group">
        <label asp-for="@Model.IdUsuarioPropietario">Nuevo Propietario</label>
        <input asp-for = "@Model.IdUsuarioPropietario" class="form-control" id="Desc">  
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>