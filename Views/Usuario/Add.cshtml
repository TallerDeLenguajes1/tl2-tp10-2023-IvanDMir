@{ 
    ViewData["Title"] = "Añadir Usuario";
}
@using tl2_tp10_2023_IvanDMir.ViewModels
@model AUViewModel

<h1>@ViewData["Title"]</h1>
<form asp-controller="Usuario" asp-action="Add">
    <div class="form-group">
        <label asp-for="@Model.Nombre">Nombre de usuario</label>
        <input asp-for = "@Model.Nombre" class="form-control" id="nombreDeUsuario">
    </div>
     <div class="form-group">
        <label asp-for="@Model.Contrasena">contrasena</label>
        <input type ="password" asp-for = "@Model.Contrasena" class="form-control" id="contrasena">
         <span asp-validation-for="@Model.Contrasena" class="text-danger"></span> 
    </div>
    <div class="form-group">
        <label for="rol">rol</label>
        <input asp-for = "@Model.Rol" class="form-control" id="rol">
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>