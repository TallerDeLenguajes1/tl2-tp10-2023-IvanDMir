@{ 
    ViewData["Title"] = "Añadir Usuario";
}

@model Usuario

<h1>@ViewData["Title"]</h1>
<form asp-controller="Usuario" asp-action="Add">
    <div class="form-group">
        <label for="nombreDeUsuario">Nombre de usuario</label>
        <input asp-for = "@Model.nombre_De_Usuario" class="form-control" id="nombreDeUsuario">
    </div>
     <div class="form-group">
        <label for="contrasena">contrasena</label>
        <input type ="password" asp-for = "@Model.contrasena" class="form-control" id="contrasena">
    </div>
    <div class="form-group">
        <label for="rol">rol</label>
        <input asp-for = "@Model.rol" class="form-control" id="rol">
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>